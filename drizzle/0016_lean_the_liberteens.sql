-- No-op: `default_content_*` on `inventory_items` and `sale_type` on `products`
-- are already created by `0015_inventory_bottle_volume.sql`.
-- Drizzle Kit re-emitted the same DDL as 0016; this file keeps the journal aligned without failing on duplicate columns.
SELECT 1;
